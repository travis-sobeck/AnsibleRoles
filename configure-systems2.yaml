# Playbook to call multiple roles to configure systems
- name: Configure systems
  hosts: awstest.eastus.cloudapp.azure.com
  become: yes

  tasks:

- name: Call Roles
  hosts: awstest.eastus.cloudapp.azure.com
  roles:
   #- hostname
   #- firewalld
   #- ntp-config
   - motd
